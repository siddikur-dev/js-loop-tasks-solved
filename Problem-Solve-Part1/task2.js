function repeatedNumberCount(number) {
  let copyNumber = [];
  for (let num of number) {
    if (copyNumber.includes(num)) {
    }
  }
  return num;
}

const numbers = [2, 32, 1, 2, 43, 2, 5];
const uniqueNb = repeatedNumberCount(numbers);
console.log("This number are ", uniqueNb);
// ------------------------------------
/**
 
     //! 🖥 VS Code শর্টকার্ট
Command Palette খুলতে → Ctrl + Shift + P

কোড ফরম্যাট → Shift + Alt + F

Fলাইন ডুপ্লিকেট → Shift + Alt + ↓

লাইন উপরে/নিচে সরানো → Alt + ↑ / ↓

মাল্টি-কার্সর → Ctrl + Alt + ↓

টার্মিনাল খুলতে/বন্ধ করতে → Ctrl + `
* / 